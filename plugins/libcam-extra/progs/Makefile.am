INCLUDES = $(GLIB_CFLAGS) $(LIBCAM_CFLAGS)

bin_PROGRAMS = camlog2jpeg

camlog2jpeg_SOURCES = camlog2jpeg.c

camlog2jpeg_LDADD = $(GLIB_LIBS) $(LIBCAM_LIBS)

man_MANS = camlog2jpeg.1

EXTRA_DIST = camlog2jpeg.1
