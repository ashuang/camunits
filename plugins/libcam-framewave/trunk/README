This package contains libcam plugins for using libcam in conjunction with the
Intel Integrated Performance Primitives (IPP).

This package expects IPP version 5.3

Description
===========

IPP can be found at 
    http://www.intel.com/cd/software/products/asmo-na/eng/302910.htm

The plugins provided in this package are:

    TODO

Installation
============

To install from subversion:
    $ autoreconf -i
    $ ./configure
    $ make 
    $ make install

To install from a tarball (.tar.gz):
    $ ./configure
    $ make
    $ make install

